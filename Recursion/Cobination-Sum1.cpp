// Cobination Sum I
// Given an array of integers(distinct) candidates and a target, return a list of all uniue combinations of candidates where the chosen numbers sum to target.
// The same number may be chosen from candidates an unlimited number of times. Two combinations are unique if the frequency of at least one of the chosen numbers is different.


// You may return the combinations in any order.
// arr[]={2,3,6,7} target=7
// Output: [[2,2,3],[7]]
// Explanation: 2+2+3=7 and 7=7

#include <iostream>
#include <vector>

using namespace std;

class Solution{
    public:
    ;
};